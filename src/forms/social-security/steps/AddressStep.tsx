import type { StepComponentProps } from "../../../components/react/forms/FormContainer";
import { FormStep } from "../../../components/react/forms/FormStep";

export function AddressStep(_props: StepComponentProps) {
  return (
    <FormStep title="What is your mailing address?">
      {/* TODO: Add AddressField for mailing address */}
    </FormStep>
  );
}
