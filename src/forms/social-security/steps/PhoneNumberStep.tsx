import type { StepComponentProps } from "../../../components/react/forms/FormContainer";
import { FormStep } from "../../../components/react/forms/FormStep";

export function PhoneNumberStep(_props: StepComponentProps) {
  return (
    <FormStep title="What is your phone number?">
      {/* TODO: Add PhoneField for phoneNumber */}
    </FormStep>
  );
}
